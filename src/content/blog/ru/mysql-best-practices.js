export default `
<p>MySQL обслуживает огромную долю продакшен-приложений в мире, и большинство проблем с производительностью, которые приписывают «базе данных», на самом деле сводятся к нескольким предотвратимым ошибкам. Разберём, что важно, когда проект перерастает игрушечную схему.</p>

<h2>Индексирование: изменение с наибольшей отдачей</h2>
<p>Без индекса MySQL сканирует каждую строку таблицы, чтобы выполнить запрос. Добавляйте индекс на любой столбец, по которому часто фильтруете или соединяете таблицы:</p>
<pre><code>CREATE INDEX idx_users_email ON users(email);</code></pre>
<p>При этом индексы не бесплатны — они ускоряют чтение, но замедляют запись и занимают место на диске. Индексируйте столбцы, от которых реально зависят ваши запросы, а не каждый столбец, по которому когда-нибудь, возможно, понадобится поиск.</p>

<h2>Нормализация и когда её нарушать</h2>
<p>Нормализованные схемы избегают дублирования данных и сохраняют их согласованность — email пользователя хранится ровно в одном месте. Но строгая нормализация может означать дорогие соединения (join) на путях с интенсивным чтением. Для высоконагруженных отчётных запросов контролируемая денормализация — например, хранение предварительно посчитанной суммы вместо пересчёта через join каждый раз — это законный компромисс, а не ошибка.</p>

<h2>Чтение вывода EXPLAIN</h2>
<p>Перед оптимизацией любого медленного запроса спросите у MySQL, как он планирует его выполнять:</p>
<pre><code>EXPLAIN SELECT * FROM orders WHERE customer_id = 42;</code></pre>
<p>Обратите особое внимание на столбец <code>type</code> — значение <code>ALL</code> означает полное сканирование таблицы, и это сигнал, что индекс там, где он нужен, отсутствует.</p>

<h2>Пул соединений (connection pooling)</h2>
<p>Открывать новое соединение с базой данных для каждого запроса — дорого, и это не масштабируется даже при небольшом числе одновременных пользователей. Используйте пул соединений — большинство фреймворков и ORM (SQLAlchemy, Django ORM) поддерживают это нативно — чтобы соединения переиспользовались, а не создавались постоянно заново.</p>

<h2>Резервные копии — не опция</h2>
<p>Продакшен-база данных без автоматических резервных копий — это вопрос не «если», а «когда» вы потеряете данные: из-за плохой миграции, ошибки или сбоя оборудования. Минимум: автоматические ежедневные резервные копии, хранящиеся отдельно от самого сервера базы данных, с процессом восстановления, который реально протестирован хотя бы раз — до того, как он понадобится в реальности.</p>

<h2>Избегание проблемы N+1 запросов</h2>
<p>Это одна из самых распространённых и незаметных проблем производительности: получение списка записей, а затем цикл по ним с отдельным запросом на каждый элемент для получения связанных данных. Список из 100 заказов не должен порождать 100 дополнительных запросов для получения каждого клиента — связанные данные нужно получать одним join или одним пакетным запросом.</p>

<h2>Практический чек-лист</h2>
<p>Для любой продакшен-настройки MySQL: индексируйте внешние ключи и часто фильтруемые столбцы, читайте EXPLAIN перед слепой оптимизацией, используйте пул соединений, автоматизируйте резервные копии и реально их тестируйте, следите за паттернами N+1 по мере роста числа запросов приложения. Эти привычки важнее любой отдельной продвинутой техники оптимизации.</p>
`;
