export default `
<p>Безопасность — это не отдельный этап, который добавляют после того, как функция заработала, а свойство того, как эта функция написана изначально. Вот основы, которые каждый backend-разработчик должен считать обязательными, независимо от языка или фреймворка.</p>

<h2>SQL-инъекции: всё ещё самая распространённая предотвратимая ошибка</h2>
<p>Никогда не формируйте SQL-запросы, подставляя пользовательский ввод напрямую в строку. Это самая распространённая и легко предотвратимая уязвимость в backend-коде:</p>
<pre><code># Опасно — никогда так не делайте
query = f"SELECT * FROM users WHERE email = '{user_input}'"

# Безопасно — параметризованный запрос
cursor.execute("SELECT * FROM users WHERE email = %s", (user_input,))</code></pre>
<p>Каждый современный драйвер базы данных поддерживает параметризованные запросы. Никаких законных причин их пропускать нет.</p>

<h2>Cross-Site Scripting (XSS)</h2>
<p>Если приложение выводит присланный пользователем контент на страницу без экранирования, злоумышленник может внедрить скрипт, который выполнится в браузере другого пользователя — украдёт cookie сессии, перенаправит пользователя или незаметно изменит то, что он видит. Всегда экранируйте вывод по умолчанию, а опцию «raw HTML» любого шаблонизатора используйте редко и осознанно, но никогда — по умолчанию.</p>

<h2>Хранение паролей</h2>
<p>Никогда не храните пароли в открытом виде и никогда не используйте быстрые универсальные хеши, такие как MD5 или SHA-256, отдельно для хранения паролей — они созданы для скорости, а это именно то свойство, которое здесь нежелательно. Используйте медленный, специально предназначенный алгоритм:</p>
<pre><code>from werkzeug.security import generate_password_hash, check_password_hash

hashed = generate_password_hash("user_password")
is_valid = check_password_hash(hashed, "user_password")</code></pre>
<p>Bcrypt, Argon2 и scrypt специально разработаны так, чтобы быть вычислительно дорогими — именно это делает подбор паролей по утёкшей базе непрактичным.</p>

<h2>Управление секретами</h2>
<p>API-ключи, учётные данные базы данных и токены никогда не должны появляться в исходном коде или попадать в систему контроля версий. Используйте переменные окружения, загружайте их библиотекой вроде <code>python-dotenv</code> при разработке, а в продакшене — полноценным менеджером секретов. Добавляйте <code>.env</code> в <code>.gitignore</code> с первого дня каждого проекта, а не после первой утечки.</p>

<h2>Валидация входных данных на границе системы</h2>
<p>Проверяйте и очищайте всё, что приходит снаружи — поля форм, параметры запросов, загружаемые файлы, payload вебхуков — в точке входа в приложение, а не глубоко внутри бизнес-логики, где легко забыть про какой-то путь. Библиотеки вроде Pydantic делают это почти бесплатным в Python:</p>
<pre><code>from pydantic import BaseModel, EmailStr

class UserInput(BaseModel):
    email: EmailStr
    age: int</code></pre>

<h2>Ограничение частоты запросов и защита от злоупотреблений</h2>
<p>Любая публичная конечная точка — форма входа, форма обратной связи, маршрут API — нуждается в ограничении частоты запросов (rate limit), иначе она в какой-то момент столкнётся с автоматизированными злоупотреблениями: подбором учётных данных, спам-отправками или попытками отказа в обслуживании. Это касается формы обратной связи небольшого личного проекта не меньше, чем корпоративной инфраструктуры.</p>

<h2>Практичный подход</h2>
<p>Чтобы избежать уязвимостей, на которые приходится подавляющее большинство реальных взломов, не нужно быть специалистом по безопасности. Параметризованные запросы, экранированный вывод, правильно хешированные пароли, секреты вне исходного кода и проверка входных данных на границе — этих пяти привычек достаточно, чтобы устранить большую часть того, чем реально пользуются злоумышленники.</p>
`;
