export default `
<p>Python заслужил репутацию лучшего первого языка программирования по простой причине: его синтаксис читается почти как обычный английский текст, но при этом остаётся достаточно мощным для продакшен-систем в компаниях вроде Google, Instagram и Spotify. Это руководство охватывает всё необходимое для написания первого настоящего скрипта.</p>

<h2>Почему стоит начать именно с Python</h2>
<p>Прежде чем писать код, полезно понять, почему именно Python. Для новичков выделяются три причины:</p>
<ul>
  <li><strong>Читаемый синтаксис</strong> — никаких точек с запятой, никаких фигурных скобок, структуру задаёт отступ.</li>
  <li><strong>Один язык, много областей</strong> — те же основы применимы к веб-backend, автоматизации, анализу данных и AI.</li>
  <li><strong>Огромная экосистема</strong> — почти для любой задачи уже существует готовая библиотека.</li>
</ul>

<h2>Настройка окружения</h2>
<p>Установите Python 3.11 или новее с официального сайта, затем проверьте установку в терминале:</p>
<pre><code>python3 --version</code></pre>
<p>В качестве редактора VS Code с расширением Python — самый распространённый выбор для новичков: подсветка синтаксиса, автодополнение и встроенный терминал в одном месте.</p>

<h2>Переменные и базовые типы</h2>
<p>Python определяет типы автоматически, поэтому объявлять их заранее не нужно:</p>
<pre><code>name = "Sanjar"
age = 24
is_developer = True
skills = ["Python", "AI", "Telegram-боты"]</code></pre>
<p>На старте почти всё покрывают четыре типа: строки, целые/дробные числа, булевы значения и списки.</p>

<h2>Управление потоком: условия и циклы</h2>
<pre><code>if age >= 18:
    print("Можно голосовать")
else:
    print("Пока не подходит по возрасту")

for skill in skills:
    print(f"Изучаю: {skill}")</code></pre>
<p>Обратите внимание на двоеточие и отступ — этот отступ не стилистический выбор, именно так Python понимает, что относится к блоку кода.</p>

<h2>Функции: первый переиспользуемый блок</h2>
<pre><code>def greet(name):
    return f"Привет, {name}!"

print(greet("Sanjar"))</code></pre>
<p>Функция принимает входные данные, что-то с ними делает и (как правило) возвращает результат. Освоив написание небольших функций, вы уже начинаете думать как программист.</p>

<h2>Первый настоящий скрипт</h2>
<p>Вот небольшая программа, объединяющая основы — игра в угадывание числа:</p>
<pre><code>import random

target = random.randint(1, 100)
attempts = 0

while True:
    guess = int(input("Угадайте число (1-100): "))
    attempts += 1
    if guess == target:
        print(f"Верно! Понадобилось попыток: {attempts}.")
        break
    elif guess < target:
        print("Слишком мало.")
    else:
        print("Слишком много.")</code></pre>

<h2>Что дальше</h2>
<p>Когда эти основы освоены, следующие шаги: писать небольшие консольные инструменты, научиться спокойно читать traceback ошибок и выбрать одно направление — веб-разработку с FastAPI или Django, скрипты автоматизации или работу с данными через pandas. Глубина в одном направлении научит больше, чем поверхностное знакомство со всеми сразу.</p>

<h2>Заключение</h2>
<p>Самый большой скачок в навыке приходит не от чтения, а от написания кода, который ломается, и его исправления. Печатайте каждый пример сами, а не копируйте, и постройте что-то маленькое и бесполезное, прежде чем пытаться построить что-то большое и полезное.</p>
`;
